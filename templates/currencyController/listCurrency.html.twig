{% extends "base.html.twig" %}
{% block title %}
	{{ parent() }}
	Liste des monnaies
{% endblock %}
{% block content %}
	<div class="container-fluid">
		<div class="row justify-content-center">
			<div class="col-12 col-md-10">
				<h1 class="text-center text-dark">Liste des monnaies</h1>
				<div class="table-responsive">
					<table class="table table-hover">
						<thead>
							<tr>
								<th width="50%" scope="col" class="text-white">Monnaies</th>
								<th width="50%" scope="col" class="text-white">Zones économiques</th>
							</tr>
						</thead>
						<tbody>
							{% for c in currency %}
								<tr class="{{ cycle(['table- primary','table-secondary'], loop.index0) }}">
									<td>{{ c.name }}</td>
									<td>{{ c.economicZone.nom }}</td>
								</tr>
							{% endfor %}
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
